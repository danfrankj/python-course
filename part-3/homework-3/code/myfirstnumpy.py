import numpy as np


def read_data(fname):
	# IMPLEMENT ME 
	# read data with one float per line into a list of floats
	return []


def elementwise_write(lst_a, lst_b, fout = "c.txt"):
	# Convert your lists to the numpy.array data type.
	# Use the normal multiplication operator * to compute the entry-wise multiplication 
	# of the vectors into a new vector and write to a file.
	return None


def norm_sub(arr_a, arr_b):
	# compute the one norm of the vector a - b.
	return 0

	
def scale(arr):
	return np.array([0])
